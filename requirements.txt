# NFL Engine — Requirements
# Run: pip install -r requirements.txt

nfl_data_py>=0.3.3
pandas>=2.0.0
pyarrow>=14.0.0
requests>=2.31.0
tqdm>=4.66.0
numpy>=1.26.0
flask>=3.0.0
gunicorn>=21.2.0   # production WSGI server (Railway/Linux); Flask dev server is local-only
