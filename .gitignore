# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Data — needed at runtime by the hosted dashboard, so it IS committed (33MB total,
# reads are local/offline). Regenerable from fetch_data.py / run_engine.py if lost.
# (Was previously ignored; un-ignored so Railway/`railway up` ship the parquet files.)

# Runtime refresh artifacts (regenerated by ml/refresh.py on the server)
data/processed/last_refresh.json
data_seed/

# PFF subscriber data — LICENSED, personal use only. Never commit or redistribute
# (the repo is on public GitHub). Rebuild locally with: python pff_import.py
data/raw/pff/*
!data/raw/pff/README.md
data/processed/pff_grades.parquet
data/processed/pff_team_grades.parquet

# Claude Code local settings
.claude/settings.local.json

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.log
